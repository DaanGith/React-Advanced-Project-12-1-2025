//Hier evt de items uit EventPage zetten

// import { Button, Card, CardBody, Image, Stack, Heading, Flex, Text } from "@chakra-ui/react";
// //import { Tag } from "./EventItemPage";
// import { useRouteLoaderData, Link, useLoaderData } from "react-router-dom";

// export const loader = async ({ params }) => {
//     const users = await fetch ("http://localhost:3000/users");
//     const events = await fetch("http://localhost:3000/events");
//     const categories = await fetch ("http://localhost:3000/categories");
  
//     return {
//       users: await users.json(), events: await events.json(), categories: await categories.json() };
//   };

// export const EventItemCard = () => {
//   const { users, events, categories } = useLoaderData();
//  return (
//     <>
    
//          <Link to={`/`}>
//          <Button bgColor="purple.400" m="5px" p="5px">
//            Click to go back
//          </Button>
//        </Link>
      
//     </>
    

//  );
// };
   
//=================================================
 {/* {events.map((events) => (
                  <Image h="md" w="100%" borderRadius="xl" src={events.image}/>
              ))} */}